# ADR-000 — <the decision, as a short noun phrase>

<!--
PA1 architecture decision record. Required: a public test checks that the four
`##` headings below are present, and the manual 30% of this assignment is the
quality of what you write under them.

Keep the headings exactly as they are. One page is enough.

The question this ADR is really asking you:

    Reading another system's raw file export is the fastest thing you can do,
    and it is wrong. Where, specifically, did it bite you — and what would you
    have built instead if this were a system you had to run for five years?

Write it after you have finished the code, while the annoyance is fresh.
-->

**Status:** accepted
**Date:** YYYY-MM-DD

## Context

<!--
What did the two files actually do to you? Be specific and concrete — name the
field, the byte, the line number. "The encoding was wrong" is not context;
"reading the file as UTF-8 turned Bērziņš into B?rzi?? while leaving the column
positions intact, so nothing threw and the report looked plausible" is.

Worth naming if you hit them: the two different encodings, the comma decimal
separator, the trailing padding, the record that was cut short, the refund.
-->

## Decision

<!--
What you built, specifically enough that I could find it in your code. Name
the function and what it does. Include the decisions that are easy to skip
over: what you did with the malformed line, and why amount is a string.
-->

## Alternatives considered

<!--
At least one real alternative with the reason you rejected it. Real options
here include: adding iconv-lite instead of using TextDecoder; parsing with a
regular expression instead of slicing by column; storing amount as a number
and formatting on output; throwing on the malformed record instead of
collecting it; asking the upstream system for JSON instead of parsing this at
all.

That last one is not a joke, and it is the alternative most worth writing up.
-->

## Consequences

<!--
What is now easier, what is now harder, and what breaks first.

Concretely: what happens to your code when the upstream system adds a field in
the middle of the record? When a name arrives that is longer than 32
characters? When someone sends the file as UTF-8 by accident? Name a cost you
accepted — a consequences section with no downside in it is not credible.
-->
